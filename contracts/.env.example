# PetriLabs Base 主网部署环境变量
# 复制此文件为 .env 并填写实际值

# ============================================
# 必需配置
# ============================================

# 部署钱包私钥（带 0x 前缀）
# ⚠️ 警告：永远不要将此密钥提交到版本控制！
PRIVATE_KEY=0x

# Base 主网 RPC URL
# 选项 1: Base 官方 RPC (免费，可能较慢)
BASE_RPC_URL=https://mainnet.base.org
# 选项 2: Alchemy (推荐，需要 API Key)
# BASE_RPC_URL=https://base-mainnet.g.alchemy.com/v2/YOUR_ALCHEMY_KEY
# 选项 3: QuickNode (推荐，需要 API Key)
# BASE_RPC_URL=https://base-mainnet.quiknode.pro/YOUR_QUICKNODE_KEY/

# BaseScan API Key（用于合约验证）
# 获取地址: https://basescan.org/apis
BASESCAN_API_KEY=YOUR_BASESCAN_API_KEY

# ============================================
# 可选配置
# ============================================

# Orchestrator 地址（平台控制地址）
# 默认为部署钱包地址，可设置为多签钱包或硬件钱包
# ORCHESTRATOR_ADDRESS=0x

# 跳过部署确认（用于 CI/CD）
# 设置为 true 时，不会等待 10 秒确认
# SKIP_CONFIRM=false

# ============================================
# 预配置常量（通常不需要修改）
# ============================================

# Base 主网 USDC 合约地址
# USDC_ADDRESS=0x833589fCD6eDb6E08f4c7C32D4f71b54bdA02913

# Base 主网 Chain ID
# CHAIN_ID=8453

# ============================================
# 安全 Checklist
# ============================================

# 部署前请确认：
# [ ] 钱包余额 > 0.05 ETH（约 $125）
# [ ] 使用 Base 主网 RPC（chainId=8453）
# [ ] USDC 地址正确：0x833589fCD6eDb6E08f4c7C32D4f71b54bdA02913
# [ ] Orchestrator 地址为安全的多签或硬件钱包
# [ ] 编译无警告（运行 npx hardhat compile）
# [ ] 私钥未提交到 Git（.env 在 .gitignore 中）

# ============================================
# 部署步骤
# ============================================

# 1. 安装依赖
#    npm install
#
# 2. 编译合约
#    npx hardhat compile
#
# 3. 复制环境变量文件
#    cp .env.example .env
#    # 编辑 .env 填写实际值
#
# 4. 运行部署脚本
#    npx hardhat run scripts/deploy-base-mainnet.js --network base
#
# 5. 验证合约（可选，也可使用生成的 verify-commands.sh）
#    npx hardhat verify --network base CONTRACT_ADDRESS [构造函数参数]
#
# 6. 保存部署地址文件（deployed-addresses.json）到安全位置
